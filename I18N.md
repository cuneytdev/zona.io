# 🌍 ZONA - Internationalization (i18n) Documentation

## Çoklu Dil Sistemi Dokümantasyonu

ZONA oyunu, **Türkçe** ve **İngilizce** olmak üzere çoklu dil desteğine sahiptir.

---

## 📁 Dosya Yapısı

```
src/utils/i18n/
├── LanguageManager.ts      # Ana dil yönetim sistemi
├── languageConfig.ts        # Dil seçenekleri config
├── index.ts                 # Export ve initialization
└── translations/
    ├── tr.ts               # Türkçe çeviriler
    └── en.ts               # İngilizce çeviriler
```

---

## 🚀 Kullanım

### Temel Kullanım

```typescript
import { i18n } from '@utils/i18n';

// Çeviri al
const title = i18n.t('menu.title');
const startButton = i18n.t('menu.startGame');

// Mevcut dili al
const currentLang = i18n.getCurrentLanguage(); // 'tr' | 'en'

// Dil değiştir
i18n.setLanguage('en');

// Dil toggle (TR <-> EN)
i18n.toggleLanguage();

// Dil adını al
const langName = i18n.getLanguageName(); // 'Türkçe' | 'English'
```

### Component'te Kullanım

```typescript
import { i18n } from '@utils/i18n';

export class MyScene extends BaseScene {
  constructor() {
    super();
    
    // Dil değişikliğine subscribe ol
    i18n.onLanguageChange(() => this.updateTexts());
  }

  private updateTexts(): void {
    this.titleText.text = i18n.t('menu.title');
    this.buttonText.text = i18n.t('menu.startGame');
  }
}
```

---

## 🗂️ Çeviri Anahtarları (Translation Keys)

### Common (Genel)
```typescript
i18n.t('common.version')  // 'v1.0.0'
i18n.t('common.loading')  // 'ZONA Yükleniyor...' | 'Loading ZONA...'
```

### Menu (Menü)
```typescript
i18n.t('menu.title')      // 'ZONA'
i18n.t('menu.subtitle')   // 'NEON VOID'
i18n.t('menu.startGame')  // 'OYUNA BAŞLA' | 'START GAME'
i18n.t('menu.settings')   // 'AYARLAR' | 'SETTINGS'
i18n.t('menu.language')   // 'Dil' | 'Language'
```

### Game (Oyun)
```typescript
i18n.t('game.score')      // 'Skor' | 'Score'
i18n.t('game.lives')      // 'Can' | 'Lives'
i18n.t('game.level')      // 'Seviye' | 'Level'
i18n.t('game.paused')     // 'DURAKLATILDI' | 'PAUSED'
i18n.t('game.pressEsc')   // 'Menüye dönmek için ESC' | 'Press ESC for menu'
```

### Game Over
```typescript
i18n.t('gameOver.title')     // 'OYUN BİTTİ' | 'GAME OVER'
i18n.t('gameOver.restart')   // 'YENİDEN BAŞLA' | 'RESTART'
i18n.t('gameOver.mainMenu')  // 'ANA MENÜ' | 'MAIN MENU'
i18n.t('gameOver.yourScore') // 'Skorunuz' | 'Your Score'
```

### Controls (Kontroller)
```typescript
i18n.t('controls.move')   // 'Hareket: ↑↓←→' | 'Move: ↑↓←→'
i18n.t('controls.pause')  // 'Duraklat: ESC' | 'Pause: ESC'
i18n.t('controls.back')   // 'Geri: ESC' | 'Back: ESC'
```

---

## 🎮 LanguageSwitcher Component

Oyunda kullanılan dropdown dil seçici:

```typescript
import { LanguageSwitcher } from '@ui/LanguageSwitcher';

// Kullanımı
const langSwitcher = new LanguageSwitcher();
langSwitcher.position.set(x, y);
scene.addChild(langSwitcher);
```

**Özellikler:**
- 🎯 **Dropdown Menu** - Tüm dilleri gösterir
- 🌍 Bayrak emojileri ile görsel
- 📝 Native dil isimleri (Türkçe, English, Deutsch...)
- ✓ Seçili dil göstergesi
- 🎨 Hover efektleri
- 🔄 Otomatik güncelleme
- 📱 Responsive tasarım

**Dropdown Özellikleri:**
- Click ile açılır/kapanır
- Global click ile otomatik kapanır
- Her dil için hover efekti
- Seçili dilin yanında ✓ işareti
- Neon purple glow border

---

## ⚙️ Özellikler

### 1. **Otomatik Dil Algılama**
İlk açılışta tarayıcı dilini algılar:
- Türkçe tarayıcı → TR
- Diğer → EN

### 2. **LocalStorage ile Kalıcılık**
Kullanıcının dil tercihi kaydedilir:
```javascript
localStorage.getItem('zona_language') // 'tr' | 'en'
```

### 3. **Reactive Updates**
Dil değiştiğinde tüm component'ler otomatik güncellenir:
```typescript
i18n.onLanguageChange((lang) => {
  console.log(`Language changed to: ${lang}`);
  this.updateTexts();
});
```

### 4. **Type-Safe**
TypeScript ile tip güvenliği:
```typescript
type Language = 'tr' | 'en';
type TranslationKeys = { ... }
```

---

## ➕ Yeni Dil Ekleme

### 1. languageConfig.ts'ye Dil Ekle
```typescript
// src/utils/i18n/languageConfig.ts
export const AVAILABLE_LANGUAGES: LanguageConfig[] = [
  // ... mevcut diller
  {
    code: 'de',
    flag: '🇩🇪',
    name: 'German',
    nativeName: 'Deutsch',
  },
];
```

### 2. Çeviri Dosyası Oluştur
```typescript
// src/utils/i18n/translations/de.ts
export const de = {
  common: {
    version: 'v1.0.0',
    loading: 'ZONA wird geladen...',
  },
  menu: {
    title: 'ZONA',
    subtitle: 'NEON VOID',
    startGame: 'SPIEL STARTEN',
    // ...
  },
  // ...
} as const;
```

### 3. Language Type'ı Güncelle
```typescript
// LanguageManager.ts
export type Language = 'tr' | 'en' | 'de';
```

### 4. Register Et
```typescript
// src/utils/i18n/index.ts
import { de } from './translations/de';
i18n.registerTranslation('de', de);
```

### 5. Hazır! 🎉
LanguageSwitcher dropdown'u otomatik olarak yeni dili gösterecek.

---

## 🎯 Best Practices

### ✅ DO (Yapılması Gerekenler)

```typescript
// ✅ Çeviri anahtarı kullan
text.text = i18n.t('menu.startGame');

// ✅ Dil değişikliklerini dinle
i18n.onLanguageChange(() => this.updateTexts());

// ✅ Tüm UI metinlerini çevir
buttonText: i18n.t('gameOver.restart')
```

### ❌ DON'T (Yapılmaması Gerekenler)

```typescript
// ❌ Hard-coded metin kullanma
text.text = 'START GAME';

// ❌ Manuel dil kontrolü yapma
if (lang === 'tr') {
  text = 'BAŞLA';
} else {
  text = 'START';
}

// ❌ Çeviri dosyasını direkt import etme
import { tr } from './translations/tr';
```

---

## 🔧 API Reference

### LanguageManager

#### Methods

**`t(key: string): string`**
- Çeviri metnini döner
- Nested keys destekler: `'menu.title'`
- Key bulunamazsa key'i döner

**`getCurrentLanguage(): Language`**
- Aktif dili döner (`'tr'` | `'en'`)

**`setLanguage(lang: Language): void`**
- Dili değiştirir
- LocalStorage'a kaydeder
- Tüm subscriber'ları bilgilendirir

**`toggleLanguage(): void`**
- TR ↔ EN arasında toggle

**`getLanguageName(lang?: Language): string`**
- Dil adını döner (`'Türkçe'` | `'English'`)

**`onLanguageChange(callback): void`**
- Dil değişikliğine subscribe ol

**`offLanguageChange(callback): void`**
- Subscribe'dan çık

---

## 📊 Çeviri Durumu

| Dil | Kod | Durum | Tamamlanma |
|-----|-----|-------|------------|
| Türkçe | `tr` | ✅ Aktif | 100% |
| English | `en` | ✅ Aktif | 100% |
| Deutsch | `de` | ⏳ Planlı | 0% |
| Français | `fr` | ⏳ Planlı | 0% |

---

## 🐛 Debugging

Console'da i18n'yi test et:

```javascript
// Global erişim (development)
window.i18n.getCurrentLanguage()
window.i18n.setLanguage('en')
window.i18n.t('menu.startGame')
```

---

## 🚀 Gelecek Planlar

- [ ] Almanca (DE) desteği
- [ ] Fransızca (FR) desteği
- [ ] RTL (Right-to-Left) dil desteği
- [ ] Pluralization (tekil/çoğul) desteği
- [ ] Interpolation (değişken ekleme) desteği
- [ ] Fallback language desteği

---

**🌍 Made with internationalization in mind for ZONA**

